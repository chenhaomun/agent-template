display_name: Figma Design-to-Code
short_description: Implement UI from Figma at high fidelity — exact values per node, token mapping, asset export, screenshot compare loop.
default_prompt: Use figma-design-to-code to implement this Figma design.
